{% extends 'base.html.twig' %}

{% block body %}

<table>
    <tbody>
        <tr>
            <th>Name</th>
            <td>{{product.name}}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{product.description}}</td>
        </tr>
        <tr>
            <th>Size</th>
            <td>{{product.size}}</td>
        </tr>
    </tbody>
</table>

{% endblock %}